package br.com.brain.grupo.repository;
import br.com.brain.grupo.models.*;

import org.springframework.data.jpa.repository.JpaRepository;

public interface GrupoDisciplinaRepository extends JpaRepository<GrupoDisciplina, Long> {
}
