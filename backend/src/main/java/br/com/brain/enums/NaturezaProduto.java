package br.com.brain.enums;

public enum NaturezaProduto {
    /** Cobrado todo mes do contrato: mensalidade, contraturno. */
    RECORRENTE,
    /** Cobrado uma vez no ano: taxa de matricula, material. */
    UNICO,
    /** Avulso: passeio, 2a chamada. */
    EVENTUAL
}
